# Training data

Put the text corpus used to train REAL DI CHAT here.

The default file is `train.txt`.

For a real training run, replace the tiny demo corpus with a substantially larger UTF-8 text dataset.
